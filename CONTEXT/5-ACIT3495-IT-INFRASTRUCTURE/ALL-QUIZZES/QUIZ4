tion 1 		0 / 1 point
Which Kubernetes Service type exposes an application externally by provisioning a cloud provider load balancer?
Question options:
Unselected
	
ExternalName
Unselected
	
ClusterIP
Unselected
	
Headless Service
Unselected
	
LoadBalancer
Selected
	
NodePort
Question 2 		1 / 1 point
What is the main purpose of a PersistentVolumeClaim (PVC) in Kubernetes?
Question options:
Unselected
	
To expose Pods outside the cluster
Unselected
	
To automatically back up container images
Unselected
	
To create a new node in the cluster
Unselected
	
To define how many replicas a Deployment should run
Selected
	
To request storage resources from available PersistentVolumes
Question 3 		1 / 1 point
Which Kubernetes object is primarily used to manage stateless application updates and maintain a desired number of Pod replicas?
Question options:
Unselected
	
Ingress
Selected
	
Deployment
Unselected
	
ConfigMap
Unselected
	
PersistentVolume
Unselected
	
ServiceAccount
Question 4 		1 / 1 point
A Kubernetes Deployment manages a stateless web application that must be reachable from outside the cluster and also needs persistent storage for uploaded files. Which combination best satisfies these requirements?
Question options:
Unselected
	
Use a DaemonSet for the web application and store uploads in environment variables
Unselected
	
Use an emptyDir volume for durable storage and expose the Deployment with a Node selector
Unselected
	
Use an init container to store files locally and expose the Deployment with a Headless Service
Selected
	
Use a PersistentVolume mounted through a PersistentVolumeClaim and expose the Deployment with a LoadBalancer Service
Unselected
	
Use a ConfigMap for file storage and expose the Pods directly with a ClusterIP Service
Question 5 		1 / 1 point
What happens to data stored on a properly mounted PersistentVolume when a Pod using it is deleted and recreated?
Question options:
Selected
	
The data persists independently of the Pod lifecycle
Unselected
	
The data is moved automatically to a ConfigMap
Unselected
	
The data is converted into an ephemeral volume
Unselected
	
The data is always deleted with the Pod
Unselected
	
The data becomes inaccessible until the node is rebooted
Question 6 		0 / 1 point
Which Kubernetes Service type is designed to expose an application on a static port on each node in the cluster?
Question options:
Unselected
	
Headless Service
Unselected
	
NodePort
Selected
	
ClusterIP
Unselected
	
Ingress
Unselected
	
ExternalName
Question 7 		1 / 1 point
In a Kubernetes application, a Deployment manages stateless web Pods, a Service exposes them internally, and a PersistentVolumeClaim is used by a separate database Pod. Which statement best describes the roles of these resources?
Question options:
Unselected
	
A Service stores database data permanently, while a PersistentVolumeClaim distributes traffic to Pods.
Unselected
	
A Deployment provides stable network access, while a Service ensures Pods are recreated if they fail.
Selected
	
A Deployment manages Pod replicas and updates, a Service provides stable access to the web Pods, and a PersistentVolumeClaim requests storage for the database Pod.
Unselected
	
A Service is required to mount storage into a Pod, while a Deployment allocates PersistentVolumes from the cluster.
Unselected
	
A PersistentVolumeClaim automatically creates multiple replicas of a Pod, while a Deployment exposes them on a cluster IP.
Question 8 		1 / 1 point
In a Kubernetes Deployment, which strategy setting is commonly used to update Pods gradually with minimal downtime?
Question options:
Unselected
	
ImmediateReplace
Unselected
	
StaticUpdate
Unselected
	
OnDelete
Selected
	
RollingUpdate
Unselected
	
RecreateAlways
Question 9 		0 / 1 point
Which field in a PersistentVolume specification indicates how much storage is available to be claimed?
Question options:
Unselected
	
capacity
Selected
	
volumeMode
Unselected
	
storageClassName
Unselected
	
accessModes
Unselected
	
claimRef
Question 10 		0 / 1 point
A Kubernetes Deployment runs a web application, and you need to prepare configuration data before the main container starts, without rebuilding the application image. The Pods should still be reachable from inside the cluster using a stable network endpoint. Which configuration best fits this initContainer pattern?
Question options:
Unselected
	
Use a StatefulSet with no shared volume between containers, and expose it only with an Ingress
Selected
	
Use a single container Pod for the app and copy configuration into it later with kubectl cp, exposed through a NodePort Service
Unselected
	
Use a Deployment with an initContainer that writes setup files to a shared volume before the app container starts, and expose the Pods with a ClusterIP Service
Unselected
	
Use a Deployment with an initContainer running in a separate Pod, and expose both Pods with an ExternalName Service
Unselected
	
Use a DaemonSet for the web app and a ConfigMap as writable startup storage, exposed through a Headless Service
message.txt
6 KB
