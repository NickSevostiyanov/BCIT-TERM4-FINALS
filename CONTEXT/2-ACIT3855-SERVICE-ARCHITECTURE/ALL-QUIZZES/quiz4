ck Sevostiyanov
Quiz 4 - Results
Attempt 1 of 1
Written Feb 5, 2026 1:46 PM - Feb 5, 2026 1:49 PM
Attempt Score	
8 / 10 - 80 %
Overall Grade (Highest Attempt)	
8 / 10 - 80 %
Question 1		
Which from the list below are examples of keywords associated with Meta Information an OpenAPI 3.0 specification?

Correct AnswerSelected
version

Correct AnswerIncorrect ResponseUnselected
security

Unselected
/artists

Unselected
responses

Unselected
parameters

Correct AnswerSelected
title

Question 2		
Which items from the list below would considered Path Items in an OpenAPI 3.0 specification?

Correct AnswerIncorrect ResponseUnselected
response codes

Incorrect ResponseSelected
servers

Correct AnswerSelected
parameters

Unselected
components

Correct AnswerSelected
responses

Unselected
security

Unselected
schemas

Question 3		
Why would we want to use components in an OpenAPI specification? Select the best answer.

Unselected
Organization

Selected
Reuse

Unselected
Supports multiple properties

Unselected
It's required

Question 4		
Select the valid types of components you might use in your OpenAPI specification from the list below

Unselected
Meta information

Selected
Parameters

Selected
Request Bodies

Selected
Responses

Unselected
Paths

Question 5		
What advantage does using OpenAPI and Connexion have over just using Flask for building our services?

Selected
Can validate requests and responses against a schema

Unselected
Can associate a Python function with an endpoint path

Unselected
Can set the port on which your service is run

Unselected
Can use the requests library to make RESTful API calls

Question 6		
OpenAPI specifications can be written in either JSON or YAML. JSON is recommended because it is easier to read and write.

Unselected	True
Selected	False
Question 7		
For the following GET endpoint from the OpenAPI 3.0 tutorial, which would be a valid request URI? Select the best answer (i.e., most complete) considering the specification below.

paths:
  /artists:
    get:
      description: Returns a list of artists 
      #  ----- Added lines  ----------------------------------------
      parameters:
        - name: limit
          in: query
          description: Limits the number of items on a page
          schema:
            type: integer
        - name: offset
          in: query
          description: Specifies the page number of the artists to be displayed
          schema:
            type: integer
      #  ---- /Added lines  ----------------------------------------         
      
      responses:
        '200':
          description: Successfully returned a list of artists
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required:
                    - username
                  properties:
                    artist_name:
                      type: string
                    artist_genre:
                      type: string
                    albums_recorded:
                      type: integer
                    username:
                      type: string

        '400':
          description: Invalid request
          content:
            application/json:
              schema:
                type: object
                properties:   
                  message:
                    type: string
Selected
https://example.io/v1/artists?limit=5&offset=10

Unselected
https://example.io/v1/artists

Unselected
https://example.io/v1/artists?limit=5

Unselected
https://example.io/v1/artists?offset=10

Unselected
https://example.io/v1/artists?limit=5&offsets=-5

Unselected
https://example.io/v1/artists?limits=5&offset=10

Question 8		
For the following GET endpoint from the OpenAPI 3.0 tutorial, what type of response would you receive on a successful query?

paths:
  /artists:
    get:
      description: Returns a list of artists 
      #  ----- Added lines  ----------------------------------------
      parameters:
        - name: limit
          in: query
          description: Limits the number of items on a page
          schema:
            type: integer
        - name: offset
          in: query
          description: Specifies the page number of the artists to be displayed
          schema:
            type: integer
      #  ---- /Added lines  ----------------------------------------         
      
      responses:
        '200':
          description: Successfully returned a list of artists
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required:
                    - username
                  properties:
                    artist_name:
                      type: string
                    artist_genre:
                      type: string
                    albums_recorded:
                      type: integer
                    username:
                      type: string

        '400':
          description: Invalid request
          content:
            application/json:
              schema:
                type: object
                properties:   
                  message:
                    type: string
Unselected
number

Unselected
string

Selected
array

Unselected
object

Unselected
boolean

Question 9		
Select the valid statements from the list below regarding the following code example:

def populate_stats():
    """ Periodically update stats """
    print(“This is my periodic processing”)
 

def init_scheduler():
    sched = BackgroundScheduler(daemon=True)
    sched.add_job(populate_stats,   
                  'interval',
                  seconds=app_config['scheduler']['period_sec'])
    sched.start()
 

if __name__ == "__main__":
        # run our standalone gevent server
        init_scheduler()
        app.run(port=8100, use_reloader=False) 
Selected
populate_stats is called periodically

Selected
Scheduled task is running in a background daemon process

Unselected
Stores the stats to a database

Selected
Period value is stored in an application configuration file

Selected
Both a scheduled job and Connexion API (on Port 8100) are running in the same application

Unselected
populate_stats would never be called

Question 10		
We might want to use query parameters in a GET endpoint to limit the amount of data returned in the response. 

Selected	True
Unselected	False

