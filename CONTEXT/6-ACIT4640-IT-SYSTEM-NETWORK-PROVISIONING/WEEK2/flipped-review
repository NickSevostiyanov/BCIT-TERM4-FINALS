Instructions
In groups of 2-3 discuss and answer the questions provided below

Have one group member record the response to each of the questions and submit them when at the end of the exercise

Include all of the group members names on your submission

- q1.
    - The SSH protocol runs on which port (number) by default?
- q2. 
    - Where are service files (units) that are user provided? Those created by a system administrator as opposed to those installed with a package.
- q3. 
    - Describe what the three systemctl commands below do  
        - `sudo systemctl enable nginx`  
        - `sudo systemctl disable nginx`  
        - `sudo systemctl mask mysql`
- q4. 
    - What is a systemd target?
- q5. 
    - Define a Bash environment variable "USER" with the value "ruby"
- q6. 
    - Write a bash script that uses command substitution to store the output of the `date` command in a variable "now".
